# Требование REQ-001

```mermaid
flowchart TD
    REQ[REQ-001\nUser Authentication]
    ARCH[Authentication Architecture]

    REQ --> ARCH

    click ARCH "../architecture/" "Open Architecture page"
```